# Copy to .env and fill in. .env is gitignored - never commit a real key.
# Until this is set to a real key, /api/checkout returns a 501 "not configured"
# error instead of pretending to take payment.
STRIPE_SECRET_KEY=sk_test_PLACEHOLDER_REPLACE_ME
